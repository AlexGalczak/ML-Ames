***When iterating through text to find words or letters,
map returns index while filter returns actual lines

Shape:

Fancy indexing:

Matrices

SCiPy library (stats)



histogram single 
bar single cat

line - time






















Pandas

Web scraping, beautiful soup

Adv, object methods

Pandas:

Series



index, set_index(4,5,6,7), set_index('col name')
reste_index

saving to csv


tmp.to_csv("tmp.csv")
tmp.to_csv("filename.csv", index=False) save without the default index

merge only 2 table
for more use concat


loc

iloc

drop row or col













